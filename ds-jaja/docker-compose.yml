services:
  command-center:
    build:
      context: ..
      dockerfile: ds-jaja/Dockerfile
    container_name: ds-command-center
    restart: unless-stopped
    environment:
      PORT: 8082
      NODE_ENV: production
    ports:
      - "8082:8082"
    volumes:
      - ./data:/app/data
