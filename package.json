{
  "name": "ds-command-center",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "ds-jaja"
  ],
  "scripts": {
    "start": "npm --workspace ds-jaja start",
    "start:firebase": "npm --workspace ds-jaja run start:firebase",
    "dev": "npm --workspace ds-jaja run dev",
    "test": "npm --workspace ds-jaja test",
    "check": "npm --workspace ds-jaja run check"
  },
  "engines": {
    "node": ">=18"
  }
}
